# قائمة التحقق قبل الرفع ✅

## قبل Build المشروع:

- [ ] تم إنشاء ملف `.env.production` من `.env.production.example`
- [ ] تم تعديل `VITE_SITE_URL` في `.env.production` ليكون رابط الموقع الحقيقي
- [ ] تم التأكد من أن `VITE_API_BASE_URL` صحيح
- [ ] تم تشغيل `npm install` (إذا كانت أول مرة)

## Build المشروع:

- [ ] تم تنفيذ `npm run build:prod` (أو `npm run build`)
- [ ] لا توجد أخطاء في البناء
- [ ] مجلد `dist` تم إنشاؤه بنجاح

## الرفع على السيرفر:

### إذا كنت تستخدم Vercel:
- [ ] تم تثبيت Vercel CLI (`npm install -g vercel`)
- [ ] تم ربط المشروع بـ Vercel (`vercel`)
- [ ] تم ضبط Environment Variables في Vercel Dashboard
  - [ ] `VITE_API_BASE_URL`
  - [ ] `VITE_SITE_URL`
- [ ] تم الرفع على Production (`vercel --prod`)

### إذا كنت تستخدم سيرفر خاص (cPanel/VPS):
- [ ] تم رفع **محتويات** مجلد `dist` (ليس المجلد نفسه)
- [ ] تم رفع الملفات إلى `public_html` أو المجلد الصحيح
- [ ] تم التأكد من وجود `.htaccess` في المكان الصحيح
- [ ] تم ضبط permissions (755 للمجلدات، 644 للملفات)

## بعد الرفع - الاختبار:

- [ ] الموقع يفتح بشكل صحيح
- [ ] الصفحة الرئيسية تعرض الأخبار
- [ ] صفحة التفاصيل تفتح عند الضغط على خبر
- [ ] الصور تظهر بشكل صحيح
- [ ] لا توجد أخطاء في Console

## اختبار المشاركة:

- [ ] فتح خبر معين
- [ ] نسخ الرابط
- [ ] اختبار في Facebook Debugger:
  - الرابط: https://developers.facebook.com/tools/debug/
  - [ ] العنوان يظهر صحيحاً
  - [ ] الوصف يظهر صحيحاً
  - [ ] الصورة تظهر صحيحاً
  - [ ] إذا لم تظهر، اضغط "Scrape Again"

- [ ] اختبار في Twitter Card Validator:
  - الرابط: https://cards-dev.twitter.com/validator
  - [ ] البطاقة تظهر بشكل صحيح

- [ ] اختبار المشاركة الفعلية على WhatsApp/Facebook

## اختبار API:

- [ ] الأخبار تُحمّل من API الحقيقي
- [ ] لا توجد أخطاء CORS في Console
- [ ] الصور تُحمّل من Backend بشكل صحيح

## إذا واجهت مشاكل:

### API لا يعمل:
1. تحقق من أن `VITE_API_BASE_URL` صحيح
2. افحص Console للأخطاء
3. جرّب فتح API مباشرة في المتصفح: `https://backend.ascww.org/api/news`
4. تأكد من أن Backend يدعم CORS

### الصورة لا تظهر عند المشاركة:
1. تحقق من أن `VITE_SITE_URL` صحيح في Environment Variables
2. افحص صفحة المصدر (View Source) وابحث عن `og:image`
3. تأكد أن رابط الصورة يعمل (افتحه في تبويب جديد)
4. استخدم Facebook Debugger واضغط "Scrape Again"
5. انتظر بضع دقائق وحاول مرة أخرى

### الصفحة تعطي 404 عند التحديث:
1. تأكد من وجود `.htaccess` أو `_redirects`
2. في Vercel: تأكد من إعدادات Rewrites في `vercel.json`

---

## ملاحظات إضافية:

⚠️ **تذكير:** Facebook يقوم بـ cache للـ Meta Tags، لذا قد تحتاج إلى:
- استخدام Facebook Debugger
- الضغط على "Scrape Again" عدة مرات
- الانتظار بضع دقائق

✅ **نصيحة:** احفظ هذه الروابط في المفضلة:
- Facebook Debugger: https://developers.facebook.com/tools/debug/
- Twitter Validator: https://cards-dev.twitter.com/validator

---

بالتوفيق! 🚀
